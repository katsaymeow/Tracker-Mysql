const inquirer = require('inquirer');
const connection = require('../connection');
const db = require("./db");
const table = require("console.table");

// this is where I plan on adding the functions to add, delete and manipulate the data. 